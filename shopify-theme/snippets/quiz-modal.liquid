<div class="quiz-overlay" id="quiz-overlay" aria-hidden="true"></div>

<div class="quiz-panel" id="quiz-panel" role="dialog" aria-modal="true" aria-label="Find your formula" aria-hidden="true">
  <button class="quiz-panel__close" id="quiz-close" aria-label="Close quiz">
    <svg width="20" height="20" viewBox="0 0 20 20" fill="none" aria-hidden="true">
      <path d="M4 4l12 12M16 4L4 16" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
    </svg>
  </button>

  <div class="quiz-progress" id="quiz-progress" aria-hidden="true">
    <div class="quiz-progress__bar" id="quiz-progress-bar" style="width: 0%;"></div>
  </div>

  <div class="quiz-content" id="quiz-content">
    <!-- Populated by quiz.js -->
  </div>
</div>
